#include <stdio.h>

/**
* main - Entry point
*
* Return: always 0 (Seccess/correct)
*/
int main(void)
{
int alphabet;

for (alphabet = 'a'; alphabet <= 'z' ; alphabet++)
{
putint(alphabet);

}
putint("\n");

return (0);
}
