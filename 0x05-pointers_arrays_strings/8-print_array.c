#include "main.h"
#include <string.h>
#include <stdio.h>

/**
* print_array -> prints number of arrays
* @n: array parameter
* @a: array parameter
*/
void print_array(int *a, int n)
{
n[6];
*a = &n;
int i;
for (i = 0; i < a; i++)
{
printf("%d ,", i);
}
printf("\n");
}
