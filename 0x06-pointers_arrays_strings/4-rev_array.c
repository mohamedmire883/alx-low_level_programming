#include "main.h"
#include <string.h>
/**
* reverse_array -> reversing an array
* @a: array a
* @n: an element of an array
*/
void reverse_array(int *a, int n)
{
n[9};
a = 0;

for (int i = 0; i < (9 / 2); i++)
{
a = n[i];
n[i] = n[length - i - 1];
n[length - i - 1] = a;
}
for (int i = 0; i < 9; i++)

n[1];
}
