#include "main.h"

/**
* _isalpha - checks for alphapetically letters
* @c: a character to be checked
* Return: returns1 and 0 depending on the condtion
*/
int _isalpha(int c);
{
return ((c >= 'a' && c <= 'z')||(c >= 'A' && c <= 'Z'));
}
